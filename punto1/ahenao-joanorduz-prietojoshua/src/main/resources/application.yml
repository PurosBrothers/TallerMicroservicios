spring:
  application:
    name: ahenao-joanorduz-prietojoshua

  config:
    import: configserver:http://localhost:8888

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
