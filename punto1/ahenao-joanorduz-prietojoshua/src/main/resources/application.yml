spring:
  application:
    name: ahenao-joanorduz-prietojoshua

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
